# Future case A — handle or hollow

Drop `input.glb` here. Do not change `ccstudio.py` first.

```powershell
.\run_test.ps1 -Input "fixtures\future\A_handle_or_hollow\input.glb" -Name "A_handle_or_hollow"
```
